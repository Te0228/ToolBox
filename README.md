# Electron Toolbox

一个基于 Electron + TypeScript + React + Vite 开发的多工具应用。

## 功能特性

- 🛠️ **多工具支持** - 可扩展的工具系统，方便添加新工具
- 📝 **JSON 编辑器** - 自动格式化、对齐、压缩 JSON 数据
- 🎨 **现代化 UI** - 深色主题，美观易用
- ⚡ **快速响应** - 基于 Vite 的快速开发体验

## 已实现的工具

### JSON 编辑器
- 自动格式化 JSON
- 可调节缩进大小（1-8 空格）
- 压缩 JSON（Minify）
- 一键复制格式化结果
- 实时错误提示

## 开发

### 安装依赖

```bash
npm install
```

### 启动开发服务器

```bash
npm run dev
```

这会同时启动 Vite 开发服务器和 Electron 应用。

### 构建

```bash
npm run build
```

构建完成后，可以运行：

```bash
npm run electron
```

## 项目结构

```
electron-toolbox/
├── electron/          # Electron 主进程代码
│   └── main.ts
├── src/               # React 应用源码
│   ├── components/    # React 组件
│   │   ├── Toolbar.tsx      # 工具栏组件
│   │   └── tools/            # 工具组件目录
│   │       └── JsonEditor.tsx
│   ├── App.tsx        # 主应用组件
│   ├── main.tsx       # React 入口文件
│   └── *.css          # 样式文件
├── index.html         # HTML 模板
├── package.json
├── tsconfig.json      # TypeScript 配置
└── vite.config.ts     # Vite 配置
```

## 添加新工具

1. 在 `src/components/tools/` 目录下创建新的工具组件
2. 在 `src/App.tsx` 中添加工具类型和渲染逻辑
3. 在 `src/components/Toolbar.tsx` 中添加工具按钮

## 技术栈

- **Electron** - 跨平台桌面应用框架
- **TypeScript** - 类型安全的 JavaScript
- **React** - UI 框架
- **Vite** - 快速的前端构建工具

## License

MIT

